/**
 * Teams content script — present so Teams pages are injectable/marked.
 * Heavy scraping runs via browser.scripting.executeScript from background.
 */
console.debug('[Curtis Bridge] Teams content script loaded');
